{
  "name": "news-comment-sentiment-analysis",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc --outDir dist",
    "clean": "rm -rf dist",
    "scrape-comments": "node dist/scrapeComments",
    "scrape-articles": "node dist/scrapeArticleUris",
    "start": "yarn run clean && yarn run build && yarn run scrape"
  },
  "dependencies": {
    "@types/puppeteer": "^1.9.1",
    "puppeteer": "^1.10.0"
  },
  "devDependencies": {
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.15.0",
    "typescript": "^3.1.6"
  }
}
